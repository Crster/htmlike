import renderMain from './render';
import expressViewEngineMain from './expressViewEngine';

export const render = renderMain;
export const expressViewEngine = expressViewEngineMain;

export default {
  render,
  expressViewEngine,
};
